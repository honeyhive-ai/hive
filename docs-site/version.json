{
  "tag": "v1.4.4",
  "name": "Hive 1.4.4",
  "url": "https://github.com/honeyhive-ai/hive/releases/latest",
  "notes": "Switching workspaces now correctly re-scopes the chats/channels view — the sidebar keyed by the folder path, which doesn't change for folderless team/room workspaces, so the previous workspace's view lingered. And every workspace (Home and any created/joined) now gets a default #general channel on activation, not just the one active at startup."
}
